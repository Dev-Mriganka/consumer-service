plugins {
    id 'java'
    id 'org.springframework.boot' version '3.5.4'
    id 'io.spring.dependency-management' version '1.1.7'
}

group = 'com.vaultsearch'
version = '0.0.1-SNAPSHOT'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

configurations {
    compileOnly {
        extendsFrom annotationProcessor
    }
}

repositories {
    mavenCentral()
}

dependencyManagement {
    imports {
        // BOM for Spring Cloud AWS
        mavenBom "io.awspring.cloud:spring-cloud-aws-dependencies:3.1.1"
        // BOM for the low-level AWS SDK
        mavenBom "software.amazon.awssdk:bom:2.27.21"
    }
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'

    // Use the official OpenSearch Java Client
    implementation 'org.opensearch.client:opensearch-java:2.12.0'
    // The transport and JSON mapping library needed by the client
    implementation 'org.opensearch.client:opensearch-rest-client:2.12.0'
    implementation 'com.fasterxml.jackson.core:jackson-databind'

    implementation 'io.awspring.cloud:spring-cloud-aws-starter'
    // SQS, DynamoDB, and Lombok dependencies
    implementation 'io.awspring.cloud:spring-cloud-aws-starter-sqs'
    implementation 'software.amazon.awssdk:dynamodb'
    implementation 'software.amazon.awssdk:dynamodb-enhanced'


    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'

    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

tasks.named('test') {
    useJUnitPlatform()
}